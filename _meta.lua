local _ = require("gettext")
return {
    fullname = _("Enhanced Crop"),
    description = _("Replaces the built-in page crop algorithm with a more aggressive one that better handles borders and margins."),
}
